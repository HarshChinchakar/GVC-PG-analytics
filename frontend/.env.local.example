# The FastAPI backend. Server-side only -- never exposed to the browser,
# because every call is proxied through Next.js route handlers.
API_BASE_URL=http://127.0.0.1:8000

# Signs nothing; used only to mark cookies secure in production.
NODE_ENV=development
